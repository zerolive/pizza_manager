<h1><%= @pizza.name %></h1>

<p>
  <ul>
    <% @ingredients.each do |ingredient| %>
      <li><%= "#{ingredient.name} - #{ingredient.price}" %></li>
    <% end %>
  </ul>
</p>

<p>
  <%= "#{t(:total)}: #{@pizza_price}" %>
</p>
